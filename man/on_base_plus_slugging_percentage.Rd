% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops.R
\name{on_base_plus_slugging_percentage}
\alias{on_base_plus_slugging_percentage}
\title{Calculate On-Base Plus Slugging Percentage (OPS)}
\usage{
on_base_plus_slugging_percentage(
  hits = NULL,
  walks = NULL,
  hit_by_pitches = NULL,
  at_bats = NULL,
  sac_flys = NULL,
  total_bases = NULL,
  on_base_percentage = NULL,
  slugging_percentage = NULL
)
}
\arguments{
\item{hits}{Numeric vector, the number of hits (required if on_base_percentage is missing).}

\item{walks}{Numeric vector, the number of walks (required if on_base_percentage is missing).}

\item{hit_by_pitches}{Numeric vector, the number of times hit by pitch (required if on_base_percentage is missing).}

\item{at_bats}{Numeric vector, the number of at bats (required for both OBP and SLG calculations).}

\item{sac_flys}{Numeric vector, the number of sacrifice flies (required if on_base_percentage is missing).}

\item{total_bases}{Numeric vector, the total number of bases (required if slugging_percentage is missing).}

\item{on_base_percentage}{Numeric vector, precomputed on-base percentage (optional).}

\item{slugging_percentage}{Numeric vector, precomputed slugging percentage (optional).}
}
\value{
Numeric vector, the OPS value.
}
\description{
This function calculates the On-Base Plus Slugging (OPS) percentage, a composite statistic used
to measure a baseball player's overall batting ability. It sums the on-base percentage (OBP) and
the slugging percentage (SLG). If not provided, the function will attempt to calculate OBP and
SLG using the provided stats.
}
\examples{
on_base_plus_slugging_percentage(hits = 50, walks = 30, hit_by_pitches = 5, at_bats = 150,
                                 sac_flys = 3, total_bases = 120)
on_base_plus_slugging_percentage(at_bats = 150, total_bases = 120, on_base_percentage = 0.320,
                                 slugging_percentage = 0.480)
}
